[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "RedExtractor"
version = "0.1.0"
description = "A flexible and fast media downloading system."
authors = [
  { name="Your Name", email="you@example.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/RedExtractor"]