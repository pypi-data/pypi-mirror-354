[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mergeweave"
version = "0.0.0"
description = "Reserved namespace for MergeWeave – agent‑native code fabric"
authors = [{name = "MergeWeave Inc.", email = "admin@mergeweave.ai"}]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
