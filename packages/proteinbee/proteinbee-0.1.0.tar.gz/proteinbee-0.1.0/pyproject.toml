[project]
name = "proteinbee"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "bhashyam.anirudh@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "biotite>=1.3.0",
]

[project.scripts]
proteinbee = "proteinbee:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = [
    "hypothesis>=6.135.6",
    "pytest>=8.4.0",
]
