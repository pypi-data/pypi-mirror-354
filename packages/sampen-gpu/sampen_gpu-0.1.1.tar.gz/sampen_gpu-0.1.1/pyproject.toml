[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sampen-gpu"
version = "0.1.1"
description = "GPU-accelerated Sample Entropy in Python"
authors = [
    { name = "Joseph Mifsud", email = "jmifsud@posteo.net" }
]
license = { text = "BSD-2 Clause" }
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "numpy",
    "numba"
]
