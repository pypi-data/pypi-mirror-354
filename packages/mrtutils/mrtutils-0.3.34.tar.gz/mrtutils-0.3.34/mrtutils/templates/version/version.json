{
    "${obj.prefix}major": ${obj.major},
    "${obj.prefix}minor": ${obj.minor},
    "${obj.prefix}patch": ${obj.patch},
    "${obj.prefix}build": ${obj.build},
    "${obj.prefix}branch": "${obj.branch}",
    "${obj.prefix}hash": "${obj.hash}",
    "${obj.prefix}version_string": "${obj.asString()}"

}