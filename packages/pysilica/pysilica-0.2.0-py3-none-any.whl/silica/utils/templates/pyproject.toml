[project]
name = "silica-env"
version = "0.1.0"
description = "Environment for silica agent"
requires-python = ">=3.11"
dependencies = [
    "heare-developer>=0.8.7",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
