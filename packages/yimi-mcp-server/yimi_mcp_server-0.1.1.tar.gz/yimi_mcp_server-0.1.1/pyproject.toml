[project]
name = "yimi-mcp-server"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shs", email = "shang.n@163.com" }
]
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.9.3",
    "requests",
]

[project.scripts]
yimi-mcp-server = "yimi_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
