[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "prune_libs"
version = "1.21.3"
description = "Python libraries for Prune."
readme = "README.md"
requires-python = ">=3.12,<4.0"
authors = [{ name = "Arnout", email = "bastien@prune.sh" }]
keywords = ["captcha", "django", "code-quality"]
urls = { Made_by = "https://prune.sh/" }
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "django>=5.2",
    "django-stubs>=5.2.0",
    "pydantic>=2.11.3",
    "pytest>=8.4.0",
    "sendgrid>=6.11.0",
    "ty>=0.0.1a8",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.package-data]
"prune_captcha" = ["templates/**/*", "static/**/*"]
"prune_libs" = ["commons/*", "logic/*", "templatetags/*"]

[tool.setuptools.packages.find]
where = ["."]
