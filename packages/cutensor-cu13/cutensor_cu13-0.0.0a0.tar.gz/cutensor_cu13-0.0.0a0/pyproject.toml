[project]
name = 'cutensor-cu13'
authors = [
    {name = 'NVIDIA Kitmaker Team', email = 'compute_installer@nvidia.com' },
]
description = 'Zero version placeholder for cutensor-cu13'
license = {text = 'Apache-2.0'}
dependencies = []
version = '0.0.0a0'

[build-system]
requires = ['hatchling']
build-backend = 'hatchling.build'
