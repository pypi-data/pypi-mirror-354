[project]
name = "mcp-server-jerryni-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.9.3",
]

[project.scripts]
mcp-server-jerryni-demo = "mcp_server_jerryni_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
