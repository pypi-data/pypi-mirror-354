[project]
name = "WeatherQuery"
version = "0.0.1"
description = "A Python package for querying weather data."
authors = [
    { name = "Your Name", email = "your@email.com" },
]
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"