[project]
name = "aida-utils"
version = "0.0.1"
description = "Random utils used at aida-lab"
authors = [
    {name = "Jan Aerts", email = "jan.aerts@kuleuven.be"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"
