[project]
name = "aiden-server"
version = "0.1.3"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.9.3",
]

[project.scripts]
aiden-server = "aiden_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
