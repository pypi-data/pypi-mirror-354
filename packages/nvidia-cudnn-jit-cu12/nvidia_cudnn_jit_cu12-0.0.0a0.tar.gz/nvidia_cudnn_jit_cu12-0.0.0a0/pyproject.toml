[project]
name = 'nvidia-cudnn-jit-cu12'
authors = [
    {name = 'NVIDIA Kitmaker Team', email = 'compute_installer@nvidia.com' },
]
description = 'Zero version placeholder for nvidia-cudnn-jit-cu12'
license = {text = 'Apache-2.0'}
dependencies = []
version = '0.0.0a0'

[build-system]
requires = ['hatchling']
build-backend = 'hatchling.build'
