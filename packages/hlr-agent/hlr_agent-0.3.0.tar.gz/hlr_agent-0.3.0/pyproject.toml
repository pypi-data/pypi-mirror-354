[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hlr_agent"
version = "0.3.0"
description = "A library for managing nodes and agent workflows"
authors = [
    { name = "David Serrano", email = "davidsd.2704@gmail.com" }
]
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.8"
dependencies = [
    "openai",
    "google-genai"
]

[project.urls]
"Homepage" = "https://github.com/DavidFraifer/HLR"