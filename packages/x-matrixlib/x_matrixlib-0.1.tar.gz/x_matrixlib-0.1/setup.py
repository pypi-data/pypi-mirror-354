from setuptools import setup, find_packages

setup(
    name="x_matrixlib",
    version="0.1",
    packages=find_packages(),
    description="A simple library to multiply matrices of different dimensions",
    author="Primes",
    install_requires=[],
)
