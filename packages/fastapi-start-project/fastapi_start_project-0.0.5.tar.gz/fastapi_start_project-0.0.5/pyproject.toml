[project]
name = "fastapi-start-project"
version = "0.0.5"
description = "Generador interactivo de proyectos FastAPI con templates, docker, y más."
authors = [{ name = "Edier", email = "edierguerra55@gmail.com" }]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "rich",
    "questionary"
]

[project.scripts]
fastapi-start-app = "fastapi_start_project.cli:main"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
