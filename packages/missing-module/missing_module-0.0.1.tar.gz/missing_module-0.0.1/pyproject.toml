[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "missing_module"
version = "0.0.1"
description = "A simple package to demonstrate dependency loading"
authors = [{name = "cdh", email = "cdh@wearehackerone.com"}]
license = {text = "MIT"}
requires-python = ">=3.7"

[project.urls]
Homepage = "https://hackerone.com/cdh"
