[project]
name = "garmin-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "J.M.Bai", email = "bai13369472336@163.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.9.3",
]

[project.scripts]
garmin-mcp-demo = "garmin_mcp_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
