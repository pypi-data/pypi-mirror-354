[tool.poetry]
name = "libb-util"
version = "0.0.1"
description = "libb-util utilities module"
authors = ["bissli <bissli.xyz@protonmail.com>"]
license = "0BSD"
packages = [{ include = "libb", from = "src" }]

[tool.poetry.dependencies]
python = "^3.10"
bump2version = "^1.0.1"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
