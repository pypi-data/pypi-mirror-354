[project]
name = "wilt_amaze_slack_node"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tools.hatch.build.targets.wheel]
packages = ["src/wilt_amaze_slack_node"]