[project]
name = "varun-portal-gun"
version = "0.109.0"
description = ""
authors = [
    {name = "Varun Yelluru",email = "vyelluru@amazon.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "typer (>=0.16.0,<0.17.0)"
]

[tool.poetry.scripts]
varun-portal-gun = "varun_portal_gun.main:app"

[tool.poetry]
packages = [{include = "varun_portal_gun", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
