[project]
name = "lomo-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Lomo", email = "153917490@qq.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.9.3",
]

[project.scripts]
lomo-mcp-demo = "lomo_mcp_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
