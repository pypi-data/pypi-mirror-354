[project]
name = "decryptify"
version = "3.0.0"
description = "PDF password recovery tool"
authors = [
    {name = "d4niis44hir", email = "eat_fir.0g@icloud.com"}
]
requires-python = ">=3.8"
dependencies = [
    "pikepdf",
    "tqdm"
]

[project.scripts]
decryptify = "decryptify.cli:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

