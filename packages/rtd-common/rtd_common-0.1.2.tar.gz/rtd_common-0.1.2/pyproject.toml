[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "rtd_common"
crate-type = ["cdylib"]
manifest-path = "rtd/common/Cargo.toml"

