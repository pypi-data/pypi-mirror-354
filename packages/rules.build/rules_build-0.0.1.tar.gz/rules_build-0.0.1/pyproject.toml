[project]
name="rules.build"
version="0.0.1"
description = "CLI & engine for AI-governed dev tooling"
authors = [{ name="Jad Bitar", email="hello@jadb.io" }]
license = { text="MIT" }
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "click",
    "pydantic",
    "pluggy",
]

[build-system]
requires = ["setuptools>=61.0","wheel"]
build-backend = "setuptools.build_meta"