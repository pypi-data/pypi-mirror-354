[tool.poetry]
name = "job-hive"
version = "0.1.6"
description = "一个极简的分布式任务模块"
authors = ["YUNHAI <bybxbwg@foxmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
redis = {version = "^5.2.1", extras = ["redis"]}
confluent-kafka = {version = "^2.8.0", extras = ["kafka"]}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
