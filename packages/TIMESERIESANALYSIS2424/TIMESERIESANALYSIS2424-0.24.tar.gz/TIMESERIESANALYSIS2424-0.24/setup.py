from setuptools import setup, find_packages

setup(
    name="TIMESERIESANALYSIS2424",
    version="0.24",
    packages=find_packages(),
    install_requires=[
        "numpy",
        "pandas",
        "matplotlib",
        "statsmodels"

    ],
)
