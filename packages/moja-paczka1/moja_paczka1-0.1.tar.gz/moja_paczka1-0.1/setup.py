from setuptools import setup, find_packages

setup(
    name='moja_paczka1',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    author='Sigma',
    description='sigma packages'
)